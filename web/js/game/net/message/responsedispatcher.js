define(function(require) {
	require("prototype");
	
	/**
	 * @fileOverview handles the dispatching of incoming requests to the 
	 * appropriate response classes
	 */
	
	var ResponseDispatcher = Class.create({
		initialize: function() {
			
			// register response classes
		}
	});
	
	return ResponseDispatcher;
});
